<div>
編集
<%= form_with model: [@occupation, @machine], url: occupation_machine_path, class: 'registration-main', local: true do |f| %>
<%= render 'shared/error_messages', model: f.object %>

  <br><br>
  <div class='form-wrap'>
    <div class="form-group">
      <div class='form-text-wrap'>
      使用装置
      </div>
      <div class='form-text-wrap'>
      <label class="form-text", for= "manufacturer">メーカー</label>
      </div>
      <%= f.collection_select(:manufacturer_id, Manufacturer.all, :id, :name, {include_blank: "---"}, {class:"select-box", id:"manufacturer"}) %>
      <div class='form-text-wrap'>
      <label class="form-text", for= "strength">静磁場強度</label>
      </div>
      <%= f.collection_select(:strength_id, Strength.all, :id, :name, {include_blank: "---"}, {class:"select-box", id:"strength"}) %>
      <div class='form-text-wrap', for= "manufacturer">
      <label class="form-text", for= "machine">装置名</label>
      </div>    
      <%= f.text_area :name, class:"input-default", id:"machine" %>
    </div>

    <div class='register-btn'>
      <%= f.submit "使用機器の編集" ,class:"registration-text" %>
    </div>
  </div><br><br>
  <%= link_to '戻る', :back%>
<% end %>
</div>